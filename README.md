# Nmap
map (Network Mapper) is a powerful utility for network discovery and security auditing. To use it, simply open your terminal or command prompt, type nmap followed by your chosen flags and the target's IP address or hostname. By default, this scans the top 1,000 most common TCP ports
